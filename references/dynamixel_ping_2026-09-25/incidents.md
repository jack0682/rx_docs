# G5.2 retained incidents

- The first ownership-probe Docker command inherited the protocol-validation image entrypoint and exited with `fixture directory required`. No probe ran. Corrected the command with explicit `--entrypoint sh` / `python`; original command-0 files remain. The six negative cases and positive surrogate then ran.
- First macOS `rx-host` check compiled but reported unused imports and two now-unreachable catch-all factory patterns after filling the last backend. Narrowed Linux imports and removed unreachable arms; no suppression.
- First delivery fixture check failed E0609: the qualification function's `configuration` variable is an ArtifactRef, not CellConfiguration. Corrected the selected-backend inspection to the existing `target` CellConfiguration argument. Original compiler output remains.
- A read-only Docker image inspection briefly returned `No such image: rx-g5-client-builder`; the next image listing showed `rx-g5-client-builder:latest` and explicit-tag runs succeeded. Cause not established; no image was recreated to hide the incident.

These are separate observations, not one inferred cause. Successful later checks apply to the stated corrected inputs only.
- First actual adapter delivery reached an old harness `FILE_SIMULATION`-only assertion. Extended that fixture composition with an explicit expected backend (default unchanged), adapter-specific observed native records and truthful qualification scope. Did not alter runtime authority or substitute a test Runtime.
- Next Python loss case completed native Ping and receipt recovery, then attempted to reuse a retired client boot after creating a new incarnation. P correctly returned UNAUTHENTICATED. Moved cross-language same-request replay before retirement and retained an explicit retired-boot refusal plus fresh-boot result query. The original failed expectation remains in python-loss-second.
- The first product helper negative tool compiled its caller under the product container's non-executable temporary mount and got EACCES on execution. Built the caller on ordinary evidence disk and mounted the completed binary read-only. No RAM-build workaround or runtime capability change.
- The first Host wrong-artifact probe placed its pinned binding at filesystem root. The existing package reader refused `named package directory required` before reaching the intended contract gate. Changed the test mount to `/negative/bindings.json`; the actual Host then refused DXL_PING_CONTRACT_REQUIRED. Original and corrected results remain.
- The private parent-check mutant intentionally fails the negative suite: a fake-socket caller reaches a confirmed Ping. This is a required negative control, not a product failure or an approach pivot. Product source/binary remained unchanged.
