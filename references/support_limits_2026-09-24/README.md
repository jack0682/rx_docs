# F12 support-limit evidence

[Decision, path classification and three-way comparison](../../docs/35_open_items_and_support_limits.md). Source: [bfd66e7a](https://github.com/jack0682/rx-solutions/commit/bfd66e7a0433340fc1d7e900989475fb4b2abb1f). Paths in text are redacted to `<workspace>`/`<user-home>`; failures and observed outcomes are preserved.

The outcome is **ENFORCEMENT_PASS_LOCK_LIFETIME_UNRESOLVED**, not a claim that the old lock flake is fixed. [Summary](summary.json), [support result](support-result.json), [exact support commands](support-commands.json). The final stress attempt failed in round0 and is retained in both the result and [raw output](final-host-stress.txt).

- Original source substitution: [normal](before-normal.txt), [script-only refusal](before-script-only.txt), [forged inventory accepted](before-forged-inventory.txt), [0/1/0 result](before-manifest-result.json). Final commands/results show the coordinated substitution refused by the actual run path, including catalog+inventory.
- Guarded boundary: [failed delivery with/without final report](guarded-delivery.txt), [actual Host runtime lock holder](host-runtime-lock.txt), [both resident writer-lock refusals](store-refusals.json), [future-schema refusal](future-schema.json), [initializer replay refusals](initializer-refusals.txt).
- Product child inheritance: [actual parent/child FD records and manager loss](daemon-fds.json). After exec the measured children have no DB/writer-lock FD; the observer reacquires both locks after manager SIGKILL while the children remain.
- Mechanism measurements: [macOS50 clean rounds](macos-stress.json), [Linux stress failure](linux-stress-failure.txt), [failure excluding only the new test](existing-tests-stress-failure.txt), [50-round control excluding the old crash-child spawner](no-spawner-control.txt), [isolated explicit-unlock counterfactual50 rounds](explicit-unlock-counterfactual.txt). Control exclusions are not changes to the normal test suite.
- [Deliberate inherited descriptor probe](inherited-descriptor.txt) / [unit probe source](inherited-descriptor-probe.rs) first establish the general lifetime condition. [Actual Command pre_exec result](actual-command-preexec.txt) / [standalone source](preexec-probe.rs) / [manifest template](preexec-probe-cargo.txt) then use the unmodified product SqliteRepository with a controlled fork-to-exec barrier, without deliberately passing its FD. To reproduce the standalone probe, place its files in an isolated crate and point the path dependency at the pinned SDK. Unsafe pre_exec instrumentation exists only in this experiment, not product code.
- [Counterfactual patch](counterfactual-only.patch) is preserved for the named STORAGE_LOCK_LIFETIME successor. It is NOT shipped code. The probe copy's SDK inventory was resealed only to build the intentional experimental changes; product SDK inventory is unchanged. The final low-level public error/lifetime change must originate in rx-platform with proper SDK regeneration.
- [macOS workspace375/0/17](macos-workspace.txt), [macOS clippy](macos-clippy.txt), [Linux workspace clippy](linux-clippy.txt), [protected baseline comparison](protected.json). Full command: `tools/cargo test --workspace --all-features --locked --target-dir ../.f12-evidence/target-macos`. No added ignored test or assertion relaxation.
- Existing six passages: [library32](library-passage.txt) / [commands](library-commands.json), [resident](resident-result.json), [resource](resource-result.json) / [commands](resource-commands.json), [manager-loss](manager-result.json) / [commands](manager-commands.json), [work](work-result.json) / [commands](work-commands.json), [replacement](replacement-result.json) / [commands](replacement-commands.json).
- Retained development/host failures: [guarded fixture without cooperative TERM handler](retained-missing-term-handler-failure.txt) correctly produced an unconfirmed exit; the fixture was corrected without weakening its assertion. [Linker stderr](retained-linker-stderr.txt) and [compiler JSON](retained-linker-json.txt) record SIGKILL during concurrent builds; no kernel OOM cause is asserted from that alone. Final build concurrency is one cargo job; test concurrency remains16.

All execution evidence is software-only, with actual Linux processes and explicit fault injection where labeled. No physical qualification or operating-area approval is inferred. Authenticated immutable provenance and storage lock lifetime remain named successors, not an unexplained “later” category or silently excluded execution paths.
