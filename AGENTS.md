# RX 문서 저장소

- 제품·설계·규범 원본은 `docs/`, 조사·검증 증거는 `references/`에서 관리한다.
- 설명은 한국어로 작성한다. 사실·제안·미결 사항과 실제 구현·모의 시험·실물 검증 범위를 구별한다.
- 제품 소프트웨어는 형제 저장소 `rx-platform`과 `rx-solutions`에 둔다. 이 문서 저장소에는 별도 제품 이미지가 없다.
- 공통/셀 계약 v1.0의 규범 파일은 구현에 맞춰 완화하지 않는다. 변경 시 영향·반례·revision·manifest를 함께 검토한다.
- 과거 단계의 검증 증거를 덮어쓰거나 현재 소스의 검증으로 바꾸어 표현하지 않는다. 새 단계의 기록을 추가한다.
- 첫 물리 셀은 NOT_COMMISSIONED다. 모의 검증을 실물 운전 허가로 승격하지 않는다.
- 기존 workspace의 `docs`·`references`는 이 저장소를 가리키는 호환 경로다. 새 문서 원본은 이 저장소에서 수정한다.
