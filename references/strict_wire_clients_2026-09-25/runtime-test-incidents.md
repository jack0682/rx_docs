The first full runtime scenario reached real commissioning, run start, part and activation. The wrong registered-surface certificate was correctly refused by P as PERMISSION_DENIED (certificate is not registered); the harness incorrectly expected UNAUTHENTICATED. Corrected only the expected error category, preserving the actual server reason and failure log. Forged-parent negative now also uses its own request key, separate from the later valid request. No runtime or SDK permission behavior changed.
