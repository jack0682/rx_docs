# G4 retained incidents

1. Initial daemon compile: helper inserted between tokio::main attribute and async main. E0728 (await outside async), no runtime executed. Fixed attribute placement. Logs initial-daemon-build.log and initial-solutions-mac-tests.log.
2. Actual ENOSPC: concurrent full macOS debug/incremental targets reached >10 GiB. macOS solutions compilation, platform clippy and Linux judge build stopped with explicit no-space-left-on-device errors. No test success inferred. Platform first test suite completed before this; failed logs enospc-*. Debug/incremental build targets removed after processes exited; logs retained. Rebuilt with debug=0/incremental=0 sequential repository builds, default test parallelism.
3. Docker image-blob EIO immediately afterward: both preserved runtime and builder image reads failed before product launch. docker-io-judge-build.log and after-injection.log retain errors. Docker Desktop restart restored access to exactly the same image IDs. No image or product database deletion; no product fix claimed. A --rm container was listed before restart; Docker showed none afterward. Do not conflate EIO with a policy refusal.

The earlier G3 E0463 crate-metadata incident remains historically cause-unestablished. This explicit ENOSPC is not evidence that G3 had the same cause.

4. First multi-area passage: 16 scenes passed, then the role-mutation fixture altered the live immutable request. Receiver refused at mailbox/immutable-publication-conflict before reading the negative judge report (0 work rows). The test expected judge-policy-denied and failed. Product enforcement held; no cross-area positive. Corrected fixture uses a separate hostile issuer-input copy and transfers only the explicitly unverified negative report; live request remains byte-identical. Source Rust and final binaries unchanged. Full corrected passage retained separately.

5. Final G1 regression attempt: runner exec session52965 returned exit120 with empty output; regression state stopped at lock building, no completed host-build log/result. Before this, disk dropped to118MiB; deleting completed duplicate binaries restored866MiB. Docker top reported container not running while ps still reported Up. Exact Python failure reason was not captured, so do not promote ENOSPC inference to an observed traceback. Not a passing/failing runtime test: G1 scenes had not begun. Original target and commands remain. Recovery: restart inconsistent engine; pin the identical Rust builder parent with only DEBUG=0 and INCREMENTAL=0 build-profile environment, retain original runtime image and unchanged passage assertions, then run one fresh full G1 passage.

The second engine restart request stalled after backend logs closed the API sockets. CLI start incorrectly reported already running while the socket was absent. After >3 minutes without shutdown progress and after TERM failed to exit, the stuck backend PID78548 was killed and Docker reopened. Engine29.7.2 returned; no image/database reset or deletion. A compact-builder invocation issued before recovery completed failed to connect (retained log); it did not run a product test. The subsequent build waits for confirmed engine availability.
