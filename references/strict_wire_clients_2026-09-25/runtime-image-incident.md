The first local validation image assembly used FROM sha256:<image ID>. BuildKit interpreted sha256 as a registry repository name and denied the pull before any build stage. Corrected to the existing local tags with before/after image-ID equality checks; no source/runtime behavior or tests changed. Original Dockerfile, command and error retained.
The next build reached native auditing but lacked the original image build's ROS environment; ldd could not resolve the existing ROS bridge libraries. The original Solutions.Dockerfile explicitly sources /opt/ros/jazzy/setup.sh before auditing. Added that same environment step, retaining full dependency audit and both failed logs. No package/library or test removed.
The first execution of the existing Host crash test failed before its child ran: its compiled CARGO_BIN_EXE path was outside the mounted executable. The test target now has its original read-only path mounted too; the unmodified crash assertions and original failure are retained.
