Publishes the existing strict-wire-v1 profile and 82 rule-authored vectors, then provides installable rxclpy and rxclcpp interfaces generated from the same content-addressed bundle. Both validate raw messages before parsing, use mTLS unary calls and preserve runtime-owned admission/results. Base Operation.Submit/Lookup stay unimplemented; examples use Cell.SubmitOperation and same-request recovery. A clean-generator defect that omitted Host configuration IDL is fixed without changing the nine RPC IDL hashes.

Validation: Rust/Python/C++ corpus passes; Python/C++ expanded-byte hashes and categories match. Two isolated mutants (duplicate singular and enum zero) fail fixed expectations. Installed wheel, external find_package consumer, actual P/H public commissioning, TLS response loss, one native effect, UNKNOWN after I/O failure, client restart and reverse-language reads passed for both languages. Full macOS workspace 408/0/16; clippy/repository checks. Adds a required Client libraries CI job.

This is G5.1 only: no robot adapter is connected; carrying a ROBOTIS product remains unproved; neither G5 SDK baseline nor five-product bundle is complete. Immediate G5.2 is a Host-owned simulated DYNAMIXEL Ping adapter. ROBOTIS dependency/manager research and retained failures are recorded in rx_docs. Related solutions branch: feature/g5-strict-wire-first-client; exact three-repository combinations are consolidated in the documentation PR.
